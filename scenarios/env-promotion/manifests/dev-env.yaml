---
# dev environment — namespace, release metadata, go-api deployment + service + ingress.
# All three environments start at the SAME baseline image (go-api:v1.0.0).
apiVersion: v1
kind: Namespace
metadata:
  name: env-dev
  labels:
    snowops.net/env: dev
    snowops.net/scenario: env-promotion
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: env-metadata
  namespace: env-dev
  labels:
    snowops.net/env: dev
    snowops.net/scenario: env-promotion
data:
  env_name: "dev"
  app: "go-api"
  declared_tag: "v1.0.0"
  promoted_from: ""
  promoted_at: ""
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-api
  namespace: env-dev
  labels:
    app: go-api
    snowops.net/env: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-api
      snowops.net/env: dev
  template:
    metadata:
      labels:
        app: go-api
        snowops.net/env: dev
    spec:
      containers:
        - name: go-api
          image: go-api:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: ENV_NAME
              value: dev
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: go-api
  namespace: env-dev
  labels:
    snowops.net/env: dev
spec:
  selector:
    app: go-api
    snowops.net/env: dev
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-api
  namespace: env-dev
  labels:
    snowops.net/env: dev
    snowops.net/scenario: env-promotion
spec:
  ingressClassName: {{.IngressClass}}
  rules:
    - host: go-api-dev.{{.DomainSuffix}}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-api
                port:
                  number: 80
