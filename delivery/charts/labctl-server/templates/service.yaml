apiVersion: v1
kind: Service
metadata:
  name: {{ include "labctl-server.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "labctl-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "labctl-server.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
