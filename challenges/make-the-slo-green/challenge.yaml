name: make-the-slo-green
displayName: "Make the SLO green"
description: >
  go-api's availability SLO is breaching: the error rate through the ingress
  is far above threshold and the burn rate is climbing. Every pod is Running
  and Ready, and nothing was deployed. Find the root cause and confirm the
  SLO recovers.
category: config
parTime: "8m"
setup:
  type: incident
  ref: service-selector-broken
grading:
  useDetectionCheck: true
hintPenalty: 5
